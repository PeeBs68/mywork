# Script to give the absolut value for a number
# absolute could be described as the distance from zero (it's always a positive value)

number = (float(input("Enter the number: ")))
absolute_number = abs(number)
print(f"original number is {number} and the absolute value is {absolute_number}")
